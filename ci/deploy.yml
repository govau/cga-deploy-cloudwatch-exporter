---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: govau/cga-cf-bosh-cli
    tag: latest

inputs:
- name: src
- name: deploy-src
- name: img

outputs: []

params:
  ENV:
  KUBECONFIG:

run:
  path: deploy-src/ci/deploy.sh